[project]
name = "iac-drift-reconciliation-saas"
version = "0.1.0"
description = "Skeleton for LLM-powered IaC drift reconciliation SaaS"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
